/**
 * DTO for user controller structure
 */

import { Request, Response } from "express";

// Define the static method signature
export interface UserContorllerDto {
  // Static method signature for 'test'
  // test(request: Request, response: Response): void;
}
